<%= form_with(model: card, local: true) do |form| %>
  <% if card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cardname %>
    <%= form.text_field :cardname %>
  </div>

  <div class="field">
    <%= form.label :cardtext %>
    <%= form.text_field :cardtext %>
  </div>

  <div class="field">
    <%= form.label :cardtype %>
    <%= form.text_field :cardtype %>
  </div>

  <div class="field">
    <%= form.label :cardset %>
    <%= form.text_field :cardset %>
  </div>

  <div class="field">
    <%= form.label :cardrarity %>
    <%= form.text_field :cardrarity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
